meta:
  name: chinese-quest-bot
  version: 1.0.0
build:
  language: python
  requirements: requirements.txt
run:
  command: ls -la && python bot.py
  persistenceMount: /data
  environment:
    - BOT_TOKEN